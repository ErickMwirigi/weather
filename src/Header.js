import React from 'react';

const Header = () => {
  return (
    <header className="header">
      <h1>Weather App</h1>
      {/* content for the header*/}
    </header>
  );
};

export default Header;
